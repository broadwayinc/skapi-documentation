# TODO

- Now table name, index name/value, tags strings are not limited from delimiter characters. (from 1.5.5)
- clientSecretRequest has more features (queue, expiration)
- update connection now has refresh